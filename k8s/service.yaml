apiVersion: v1
kind: Service
metadata:
  name: spring-boot-hello-world
  labels:
    app: spring-boot-hello-world
spec:
  type: ClusterIP
  selector:
    app: spring-boot-hello-world
  ports:
    - name: http
      port: 80
      targetPort: 8080
